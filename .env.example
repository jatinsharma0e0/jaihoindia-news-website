# ── Frontend Environment Variables ──────────────────────────────────────
# Copy to .env.local for local development

# Your Render backend URL (set this in Vercel project settings too)
VITE_BACKEND_URL=https://your-backend.onrender.com

# Local dev override (leave blank to use localhost:5000)
VITE_API_URL=http://localhost:5000/api

# Supabase Configuration
# Note: These are safe to use without VITE_ prefix because envPrefix is set in vite.config.mts
SUPABASE_URL=https://ffffnasgzkmyggoobbrv.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_0qjk3zxeVphPU3juOiM5lg_pM5QiEKC
