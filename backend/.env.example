# ── Backend Environment Variables ───────────────────────────────────────
# Copy to backend/.env for local development
# Set ALL of these in your Render dashboard (Environment tab)

# Server
PORT=5000
NODE_ENV=production

# Supabase Configuration (Backend - Secret Key)
SUPABASE_URL= URL_KEY_VAlue
SUPABASE_SECRET_KEY= VALUE

# JWT
JWT_SECRET=change_this_to_a_strong_random_secret

# External APIs
NEWSDATA_API_KEY=your_newsdata_api_key
YOUTUBE_API_KEY=your_youtube_api_key
YOUTUBE_CHANNEL_ID=your_channel_id

# CORS — set to your Vercel frontend URL after first deploy
FRONTEND_URL=https://your-app.vercel.app

# Cache
REFRESH_INTERVAL_MINUTES=60
